import loading from './loading.gif'

const Loading = () => {
	return (
		<img src={loading} alt="spinner" style={{ display: 'block', padding: '19px 0px 0px', margin: '0 auto' }} />
	)
}

export default Loading;