import spinner from './1494.gif'

const Spinner = () => {
	return (
		<img src={spinner} alt="spinner" style={{ display: 'block', padding: '98px 0px',margin:'0 auto' }} />
	)
}

export default Spinner;